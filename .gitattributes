# 统一换行符处理。
#
# 本机 Git 配置了 autocrlf，若不固定规则，跨机同步时容易出现整文件“被修改”的噪音。
# 这里规定：仓库内统一存 LF；Windows 专用脚本保持 CRLF；二进制资源不做转换。

* text=auto eol=lf

*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

*.png binary
*.jpg binary
*.jpeg binary
*.avif binary
*.tga binary
*.ico binary
*.exe binary
*.dll binary
