# Steam Screenshot Manager

一个优先在本地运行的 Steam 截图管理应用：自动发现截图，按游戏整理和统计，将原件备份到用户自己的 WebDAV，并在新电脑恢复。

**当前状态：工程基础阶段 工程骨架已落地并验证通过（开发运行与打包产物均可启动、数据库跨重启保留、渲染层无法访问文件系统）；Steam 扫描、图库与 WebDAV 尚未实现。**

2026-09-20，用户确认采用 **Electron + TypeScript + 本地 SQLite**，项目存放于 `C:\LocalSpace\Projects\My-Proj\original\steam-screenshot-manager`。同日完成规划基线与真实数据勘测，并建立 Git 仓库（GitHub 身份，本地提交，无远端）。工程基础阶段 实施结果见 [工程基础阶段 实施报告](docs/implementation-reports/工程基础阶段.md)。

## 核心目标

1. 提取本机多个 Steam 账号、多个游戏的截图，支持高质量副本和手动补充目录。
2. 游戏列表、截图浏览、数量与体积统计、按规则导出。
3. 绑定标准 WebDAV，同步新增图片并可靠校验；不依赖 NAS、自建业务服务或 Steam 登录。
4. 新电脑只需连接同一远端图库，即可恢复选定游戏的截图，不依赖旧电脑仍在线。

## 首版边界

首版面向 Windows x64，采用 Electron、TypeScript、React、本地 SQLite。React 是界面实现建议；Electron、TypeScript、本地 SQLite 是已确认约束。先完成离线采集和图库，再接 WebDAV，最后补后台可靠性。

Steam 登录、在线完整游戏库、Linux/Steam Deck、NAS 网页图库属于后续增强。首版不做存档、游戏安装包、录像、公开分享、多用户服务、端到端加密或云端永久删除。恢复图片到普通目录，不承诺自动恢复 Steam 自带截图管理器的索引。

实测该生活机上已存在 **44.6 GB Steam 录屏与剪辑数据**，属于首版范围外，仅记录在[真实数据快照](docs/evidence/real-data-structure.md)，不因此扩大首版。

## 开发与运行

环境要求：Node.js ≥ 22.12、pnpm 11.15（版本写在 `package.json` 的 `packageManager`）。

| 命令 | 作用 | 说明 |
|---|---|---|
| `pnpm install` | 安装依赖 | 依赖的构建脚本在 `pnpm-workspace.yaml` 里逐项声明放行与否 |
| `pnpm dev` | 开发运行 | 渲染层热更新；窗口内是 启动自检面板 |
| `pnpm typecheck` | 类型检查 | 主进程/预加载与渲染层两份配置分别检查 |
| `pnpm test` | 单元测试 | Vitest，覆盖迁移、事务、路径校验、设备 ID、IPC 契约 |
| `pnpm build` | 构建 | 产物在 `out/`（主进程与预加载为 CommonJS） |
| `pnpm self-check` | 构建后自检 | 在真实 Electron 运行时里写库读回并探测渲染层边界，报告写入用户数据目录的 `self-check.json` |
| `pnpm package` | 打包 | Windows 安装包与便携版输出到 `release/` |
| `pnpm package:dir` | 免安装打包 | 只产出 `release/win-unpacked/`，用于快速验证 |

运行时事实（工程基础阶段 实测）：

- **SQLite 使用 Electron 内置的 `node:sqlite`**，没有任何原生模块，也不需要 `electron-rebuild`。
- 应用数据位于 Electron 用户数据目录（Windows 为 `%APPDATA%\steam-screenshot-manager`），
  内含 `app.sqlite3`、`device.json`、`logs/`。真实图片与凭据不写入安装目录或源码目录。
- `.npmrc` 里的 `electron_mirror` 指向 npmmirror：本机实测直连 GitHub Releases 长时间无进展，
  换镜像后约 4.7 MB/s。网络条件更好的环境可以删掉该项回到默认源。

## 实施者阅读顺序

| 文档 | 内容 |
|---|---|
| [产品需求](docs/product-plan.md) | 用户流程、功能、统计口径、首版范围 |
| [技术架构](docs/architecture.md) | 进程与模块、数据模型、采集、凭据、接口边界 |
| [同步协议](docs/sync-protocol.md) | 远端布局、记录格式、去重、提交顺序、恢复、失败处理 |
| [实施计划](docs/implementation-plan.md) | 阶段任务、先后依赖、完成标准、交付要求 |
| [审核清单](docs/review-checklist.md) | 数据完整性、真实恢复、兼容性、性能和审核证据 |
| [调研快照](docs/research-2026-09-20.md) | 最初资料、来源链接及当时实测边界，仅供背景参考 |
| [真实数据快照](docs/evidence/real-data-structure.md) | 已接入的真实 Steam 数据：目录结构、字段、体量与实测更正；含本机证据副本 |

当前需求以产品需求为准；协议细节以同步协议为准；调研快照保留历史建议，不覆盖后续已收敛的规划。尤其远端存储已从“共享内容路径＋兼容回退”收敛为“设备独立不可变对象”，原因见同步协议。

## 不可破坏的约束

- Steam 来源只读：复制原件，不移动、覆盖、重压缩，不修改 Steam 的 VDF。
- 原始字节完整保存；缩略图是可丢弃缓存，JPG 和高质量版本不自动互相替代。
- 本机文件消失不传播远端删除。首版没有跨端永久删除和自动清理无引用对象。
- 同名不同内容不覆盖；相同账号、游戏、内容指纹按同一图片统计。
- 完整性验证通过才显示对应完成状态；上传请求成功不等于远端已校验。
- SQLite 只在本机使用，不把数据库文件当作多设备同步媒介。
- 密码、访问令牌、设备绝对路径不进入远端清单、普通日志或诊断包。
- 无网络、无 API key、游戏名未知时，仍然能保存本地截图。

## 给 DeepSeek 的任务入口

收到用户明确的实施指令后，按上述顺序阅读文档，从实施计划的 工程基础阶段 开始，依次交付 离线采集与归档阶段–后台可靠性阶段。当前文档不是“已实现”的证明。范围内的常规依赖选型、错误修复和相关验证可自行完成；改变数据保护规则、增加服务端或新增同步删除语义需要另行说明范围。

用户只要求规划时，不初始化应用、不安装依赖、不运行图片搬迁或远端写入。进入实施后，也不要未经用户授权提交、推送、部署或向公共站点上传图片。对真实图库和 WebDAV 的写入必须在用户指定的测试范围内执行；没有真实测试条件时继续完成隔离测试，如实标记剩余证据。

项目尚未配置 OpenSpec；本轮纯规划不创建变更提案。后续 工程基础阶段 应按执行环境适用的 OpenSpec 规则补齐项目内流程，并复用本套需求，避免重新发明另一套产品范围。不要修改无关项目或全局配置。

## 交付与审核

每个阶段更新任务状态，并提交一份 Markdown 实施报告：改动摘要、实际执行的命令与结果、未验证项、已知风险。完整模板见实施计划。没有证据不得将阶段标为通过。

最终审核重点是“电脑 A 采集 → WebDAV 保存 → 干净环境 B 重建 → 每个原件哈希一致”，并检查同名冲突、误删隔离和断网恢复。两套隔离用户数据目录只能证明逻辑隔离恢复；声称跨机器验证需要两台机器的证据。

## 现有证据

2026-09-20，前期已核实开发机（`C:\LocalSpace\softwares\Steam`）可以发现 Steam 安装目录、读取游戏安装清单；该机 userdata 下**没有截图**（无 `760` 目录，`localconfig.vdf` 无截图相关字段）。

同日接入另一台电脑（局域网 `另一台电脑`）的完整 D 盘，取得真实截图样本：**2 个账号、48 组游戏目录、6,079 张原图 / 3.32 GB**，已逐张计算 SHA-256 并固化到 `docs/evidence/`（两轮独立采集哈希完全一致）。该样本**全部为 JPEG**，没有 PNG/AVIF/TGA 与 HDR 内容。

仍然**没有**执行截图上传、远端写入或双端恢复，也**没有**真实 WebDAV 端点与第二台可控机器。实际开发时应重新检查运行环境，不能把历史路径当作产品默认值。

同日完成 工程基础阶段 工程骨架并取得实测证据（开发运行与打包产物均可启动、数据库跨重启保留、渲染层 `require`/`process` 不可用且无法读取文件系统）。按实施计划 2.1 的结论层级，工程基础阶段 只能声称到**隔离验证通过**；真实样本核数（离线采集与归档阶段）、真实 WebDAV（WebDAV 上传阶段）与跨电脑（新环境恢复阶段）仍未开始。详见 [工程基础阶段 实施报告](docs/implementation-reports/工程基础阶段.md)。
