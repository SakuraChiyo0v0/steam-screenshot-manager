# npm / pnpm 配置。
#
# electron_mirror：Electron 运行时二进制的下载源。
# 默认源是 GitHub Releases，本机实测直连长时间无进展（手动执行 install.js 十分钟未完成），
# 改用 npmmirror 镜像后实测约 4.7 MB/s。若在网络条件更好的环境，可删除此项回到默认源。
electron_mirror=https://registry.npmmirror.com/-/binary/electron/
