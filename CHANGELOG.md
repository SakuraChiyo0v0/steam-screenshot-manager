# 更新日志

本文件记录每个版本的可见变化，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

版本号遵循语义化版本：向后兼容的功能新增进次版本号，缺陷修复进修订号。
**从 0.2.0 起，每次发布都同步更新本文件与 `package.json` 的 `version`，并在提交信息里带上版本号。**

## [0.4.0] - 2026-09-21

### 新增

- **没有图库目录也能有预览缓存**：设了图库时预览仍放图库的 `cache/`（跟着图库迁移）；没设图库时放用户数据目录的 `preview-cache/`。此前预览只在归档过图库后才生成，未归档的用户只能一直解码 4K 原图。
- 预览排程不再要求"已归档"：只要来源目录里有原件就排预览（图库副本与来源文件都算解码源）。

### 变更

- 预览宽度 800 → **1000px**：相册卡片约 400px 宽，2 倍屏需要约 800px，留到 1000px 更稳；体积仍远小于原图（实测约 50KB/张 vs 原图数百 KB 到 2MB）。
- 预览缓存相关函数的参数由"图库根目录"改为"缓存根目录"，解析逻辑集中在 `resolvePreviewCacheRoot`。

### 验证

- 真机（默认 profile，无图库目录）：浏览相册后 `%APPDATA%\steam-screenshot-manager\preview-cache\` 自动出现 `previews/` 与 `minis/`，实测 21 张 / 1.07MB（原图同批约 10MB 以上）。
- 协议改为返回预览：同批图片 `naturalWidth` 从 2560/4096 变为 **1000**，卡片尺寸下依然清晰（400px 卡片在 2 倍屏需约 800px）。
- 设置页「预览缓存」统计经 IPC 核对一致（count 21 / bytes 1118110 / generated 21）。
- 单元测试 187 条通过（新增 4 条：缓存根目录解析、尺寸分目录、宽度下限）。

## [0.3.0] - 2026-09-21

### 新增

- **图片优先原图**（设置 → 日常使用，默认开）：相册卡片与游戏封面直接显示高清原图，不再回退到 Steam 自带的约 200px 缩略图。这个开关关闭后才优先用来源缩略图，适合图库很大、以省内存为先的场景；查看器的缩略图带尺寸很小，始终用轻量图。
- **相册滚动到底自动加载**：去掉需要手点的「加载更多」按钮，滚到接近底部时自动取下一页；加载失败时仍保留手动重试入口。

### 变更

- 相册底部改为状态提示：「正在加载更多…」「已加载 N 张，继续向下滚动会自动加载」「已全部加载 N 张」。

## [0.2.3] - 2026-09-21

### 修复

- **423 Locked 未按可重试处理**：真实 NAS 在自身索引/杀毒扫描期间会短暂返回 423，此前被当成"意外状态"直接记失败且不排重试。现在归入可重试（退避重试）。

## [0.2.2] - 2026-09-21

### 修复

- **真实 WebDAV 上写入失败**：上传对象与记录前没有创建父集合，真实服务会返回 409（自建隔离测试服务会自动建父目录，掩盖了这一点）。现在写入前逐级创建父集合，并缓存已确认的层级避免重复请求。
- **无法识别"目录已存在"**：不同服务端对已存在的目录返回 201 / 405 / 301，而请求层遇到 4xx 会先抛错，调用方的容错拿不到状态码。现在请求层支持显式容忍指定状态码。
- **基础路径不会被创建**：用户填写的 WebDAV 地址若带路径（如 `https://host/volume1/@tmp/backup`），该路径此前被假设为已存在。现在把地址拆成源与根路径，并逐级创建。
- **409 未归类**：远端父目录不存在时给出可读的路径错误，而不是"意外状态"的内部错误。

### 新增

- **凭据代持**：用户名与密码都留空时不发送 `Authorization`，便于把认证交给前置网关（例如本机端口镜像或统一登录网关代填凭据）。

### 验证

- 真实第三方 WebDAV（自建 NAS，经本机端口镜像）：兼容探测 5/5 通过；上传并逐张读回校验成功；从该远端恢复到空白图库成功，内容校验 0 不符。
- 自建隔离测试服务改为"父集合不存在时返回 409"，与真实服务行为一致，避免同类问题再次被掩盖。
- 单元测试 177 条通过。

## [0.2.1] - 2026-09-21

### 修复

- **恢复把损坏的已有副本当作完成**（验收 P1）：原先只看数据库存在标志，不校验文件内容；被改动、截断或删除的副本都会被记为「跳过成功」。现在跳过与补记录前都校验「存在 + 字节数 + SHA-256」，不合规时先隔离坏文件再重新下载，不覆盖用户文件。
- **复制的输出流错误没有及时接管**（验收 P1）：手工 write/drain 的写法在目标不可写时抛出未捕获的 'error' 事件，外层 	ry/catch 抓不到。改用 stream/promises.pipeline，错误变成可捕获的 Promise 拒绝，失败时销毁流并清理临时文件。

### 验证

- 回归测试 +5（共 174 条）：副本完好跳过不下载、被改动必须重下并隔离、缺文件重下、无行且损坏不补记已校验；复制到不可写目标以拒绝方式失败。
- 真实数据小样本：归档整库 0 失败；空白图库整库恢复；制造「内容被改动 / 文件被删除 / 清掉数据库行」三种故障后重跑，只修复受损的少数资产，其余全部跳过，核对内容不符或缺失为 0。

## [0.2.0] - 2026-09-21

### 新增

- **本地收集（归档）**：把来源截图复制进独立图库。流式复制同时计算 SHA-256 并与索引指纹核对；同一文件系统内临时文件改名发布；目标已存在时校验内容（相同补记录、不同拒绝覆盖）；写本地说明文件与受管副本记录。支持增量收集、幂等重跑、中断恢复与启动对账（缺失标记、临时文件清理）。
- **按规则导出**：按游戏名／年份／AppID／平铺四种布局导出，处理 Windows 非法字符、保留名与长度，冲突保留两份不覆盖，写出后重新校验指纹。
- **WebDAV 单向备份**：远端图库布局（`library.json` + 设备独立不可变对象 + 记录最后发布）。上传对象 → 读回校验 SHA-256 → 发布不可变记录 → 读回逐字段校验，**全部通过才标记已备份**。断点续传、有界退避、429 尊重服务端 `Retry-After`、认证失败暂停整轮；凭据用系统加密存储，断开只删凭据并保留备份历史。
- **新电脑恢复**：读取远端清单、按游戏选择恢复、下载后校验、结果与归档同构，复用同一套幂等与续传规则。
- **日常使用**：后台自动收集（增量扫描 → 归档 → 可选上传）、系统托盘（状态与快捷入口）、关闭窗口留在托盘、开机自启。
- **预览缓存**：用系统内置图像能力生成 800px 预览与 200px 迷你图，零新增依赖；相册与查看器缩略图带按尺寸取图；归档后自动预热。
- 应用与托盘图标（零依赖生成）、设置页「日常使用」区块、查看器显示真实的远端备份状态。

### 修复

- 验收发现的三处缺陷：加载更多重复追加、查询失败被旧列表遮住、图片加载失败未切换不可用状态；以及卡片内嵌套按钮。
- 缩略图与原图共用失败状态，导致缩略图损坏时正常原图被隐藏。
- `ssm-asset` 协议用手工流返回文件，请求被取消时泄漏句柄并耗尽连接，导致图片请求永久 pending。
- 远端记录的「原文件名」误用图库哈希名。
- 断开远端会丢失备份历史，并导致换机后整个图库重传。
- 归档失败落库用错对象 id，掩盖真实失败原因。
- 独立图库在来源消失后完全无法显示（图片定位与可用性只看来源目录）。
- 相册图片发虚：预览生成后不自动替换；冷图库可见图片排在预热队尾，等待时间过长。

### 变更

- 相册网格改用缩略图通道（自建预览优先），卡片离屏跳过渲染，查看器缩略图带窗口化（只渲染当前项附近的有限张数）。
- 去掉模态背景毛玻璃：它要求栅格化整页，实测查看器阶段多占约 200 MB 显存。
- 数据库 schema 2 → 5：新增受管副本、远端连接与远端对象、资产原文件名。

### 验证

- 单元测试 169 条通过；`typecheck` 与构建通过。
- 离线真实样本图库逐条对账：资产数量与内容指纹与采集基准完全一致（0 缺失、0 多余）。
- 隔离 WebDAV 真实 HTTP 端到端：全量上传并逐张读回校验；故障注入（429、5xx、响应截断、PUT 只存一半、登录页、认证失败）逐项确认状态与恢复。
- 空白用户数据目录 + 空白图库完成整库恢复，内容与采集基准一致，且不依赖旧电脑。
- 内存：相册阶段 −103 MB（GPU −33%），查看器阶段 −220 MB（GPU −53%）。
- 安装包级升级：用 0.1.0（schema 2）的数据库升级到本版本，资产、游戏、来源与设置全部保留。

### 已知限制

- 真实第三方 WebDAV 端点未验证（仅在隔离服务上验证）；两台真实电脑之间的端到端恢复未验证。
- 冷图库首次浏览有约 1~3 秒的「先小图后清晰」窗口。
- 未安装且本地无名称来源的游戏显示为「未知游戏（AppID）」；真实名称需要解析 `appinfo.vdf` v29 格式。
- 远端孤儿对象（重复上传留下的旧对象）尚无保留与清理策略。
- 破坏性操作（批量删除）与双向同步未实现，按产品需求留待单独设计。

## [0.1.0] - 2026-09-20

### 新增

- 工程骨架与打包链路：Electron + TypeScript + React + 内置 SQLite（`node:sqlite`），运行时依赖为零；开发运行与打包产物均可启动，数据库跨重启保留，渲染层无法访问文件系统。
- 图库界面：游戏库／全部截图／同步中心／设置四个页面，日间与夜间模式，等比例图库与大图查看器。
- 真实图库索引接入：Steam 来源发现（注册表、手动目录、多账号）、扫描入库（截图目录为主、VDF 索引为辅、排除缩略图、流式哈希、幂等）、图库查询与 `ssm-asset` 协议按 assetId 供图（渲染层拿不到文件路径）。

### 验证

- 开发运行与打包产物启动自检；渲染层能力边界探测；真实样本逐张哈希对账。

[0.4.0]: #040---2026-09-21
[0.3.0]: #030---2026-09-21
[0.2.3]: #023---2026-09-21
[0.2.2]: #022---2026-09-21
[0.2.1]: #021---2026-09-21
[0.2.0]: #020---2026-09-21
[0.1.0]: #010---2026-09-20
