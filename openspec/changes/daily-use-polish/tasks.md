# 阶段 5 实施任务：日常使用完善

## 1. 预览缓存

- [x] 1.1 预览生成：800px JPEG 写入 `cache/previews/<sha256>.jpg`，用内置 nativeImage，零新增依赖。验证：批量生成 2,918 张、0 失败。
- [x] 1.2 请求路径：缩略图按「自建预览 → 来源缩略图 → 原图」取值，并在回退时入队补齐。验证：网格实测请求 `ssm-asset://thumb/`。
- [x] 1.3 后台限速补齐：25 张/秒，避免同步解码阻塞界面。验证：浏览时自动补齐（恢复图库中已有 1 张由队列生成）。
- [x] 1.4 体积收益量化：127.4 MB vs 原图 1,619.3 MB，平均 44.7 KB vs 568 KB（12.7 倍）。验证：全量统计 + 逐文件抽查。
- [x] 1.5 批量生成工具与统计接口。验证：`--build-previews` 报告 + 设置页显示缓存统计。

## 2. 后台自动收集

- [x] 2.1 按间隔执行「增量扫描 → 归档 →（可选）上传」，启动后延迟 2 分钟首轮。验证：真机日志 `扫描 2922 个文件 · 新归档 0（3.1s）`。
- [x] 2.2 已有任务在跑时跳过本轮，不排队不叠加。验证：代码路径（检查三类任务状态）。
- [x] 2.3 单步失败只记录不打断；没有登记来源时直接结束。验证：代码路径。
- [x] 2.4 设置项与校验：间隔（5–1440 分钟）、自动备份、开关。验证：设置往返写入数据库。

## 3. 托盘与启动设置

- [x] 3.1 托盘图标（自生成）与菜单：状态文本、显示窗口、立即收集、退出。验证：启动日志 `[托盘] 已创建`。
- [x] 3.2 关闭窗口留在托盘，任务继续执行。验证：代码路径 + 设置项往返。
- [x] 3.3 开机自启跟随设置即时生效。验证：设置变更调用 `app.setLoginItemSettings`。
- [ ] 3.4 托盘菜单项的手工/自动化点击验证（本轮只验证了创建与状态文本）。

## 4. 应用图标与打包

- [x] 4.1 零依赖生成 `build/icon.png` 与 `build/tray.png`。验证：生成成功且图形正确。
- [x] 4.2 打包配置使用自有图标，托盘图标经 `extraResources` 随包携带。验证：配置已写入。

## 5. 验证与交付

- [x] 5.1 `pnpm typecheck` / `pnpm test`（165 条）/ `pnpm build` 全绿。
- [x] 5.2 预览缓存与自动收集的真机验证（见实施报告第 2、3 节）。
- [x] 5.3 设置界面与数据库往返验证。
- [x] 5.4 产出实施报告并提交。
- [ ] 5.5 安装包级升级验证（重新打包并在旧版本之上安装）。

## 6. 遗留（不属于本阶段）

- [ ] 6.1 真实第三方 WebDAV 验证（`nas` 主机不可达，缺凭据）。
- [ ] 6.2 两台真实电脑之间的端到端恢复。
- [ ] 6.3 批量操作（多选导出/归档）与破坏性操作的独立设计。
- [ ] 6.4 远端孤儿对象保留与清理策略。
- [ ] 6.5 `appinfo.vdf` v29 字符串池格式，用于未安装游戏的真实名称。
