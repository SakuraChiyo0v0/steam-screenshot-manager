# 相册发虚修复任务

## 1. 定位

- [x] 1.1 确认当前构建的相册图片尺寸：800px 预览，卡片 396px，不发虚。
- [x] 1.2 对比反馈截图所在实例：旧打包版本（schema 2、无 `local_copies`、游戏名为原始 key）。
- [x] 1.3 确认当前构建仍存在的缺陷：冷图库回退到约 200px 的来源缩略图，且生成预览后不自动替换。

## 2. 修复

- [x] 2.1 新增 `preview:ready` 事件（IPC + preload + 渲染层订阅），图片自动换成清晰预览。验证：图片地址出现 `?retry=N`。
- [x] 2.2 归档结束后把缺预览的资产入队预热。
- [x] 2.3 队列按实测解码耗时自适应让出（忙 x 让出 x，约 50% 占用）。
- [x] 2.4 回退顺序保持「预览 → 来源缩略图 → 原图」；记录"直接返回原图"不可行的实测结论。

## 3. 验证与交付

- [x] 3.1 热图库相册图片为 800px；队列 `pending`/`generated` 正常变化。
- [x] 3.2 修复队列占空比后首屏 200 张全部正常加载（此前 0 张加载）。
- [x] 3.3 `pnpm typecheck` / `pnpm test`（169 条）/ `pnpm build` 通过。
- [x] 3.4 产出实施报告并提交。
- [ ] 3.5 冷图库首屏"糊 → 清晰"的像素级前后对比（受 Chromium 缓存干扰，未稳定复现）。

## 4. 遗留

- [ ] 4.1 冷图库预热速度优化（应用内队列慢于 CLI 批量；可考虑空闲时提速或首次导入后自动批量生成并给出进度）。
- [ ] 4.2 设置页提供"生成预览缓存"入口与进度，便于用户主动预热。
