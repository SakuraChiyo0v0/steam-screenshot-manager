# 阶段 3 实施任务：WebDAV 单向备份

## 1. 数据与契约

- [x] 1.1 迁移 v4：`remotes`、`remote_objects`（唯一键 remote_id + asset_id）。验证：启动日志 `schema=4`，旧库可升级。
- [x] 1.2 shared 契约：远端连接/状态/上传 DTO、IPC 通道与进度事件、输入校验。验证：typecheck 与 IPC 校验用例。

## 2. WebDAV 客户端

- [x] 2.1 方法子集：MKCOL、PROPFIND(Depth:1)、PUT、GET、DELETE（仅自建测试文件）。验证：隔离测试服务端到端。
- [x] 2.2 路径安全：拒绝绝对路径、上级目录、空字节；中文与空格正确编码。验证：单元测试。
- [x] 2.3 重定向策略：跨站重定向拒绝，不把凭据带到别的源。验证：代码路径与错误码 `DAV_REDIRECT_REJECTED`。
- [x] 2.4 响应识别：期待 WebDAV 却拿到 HTML（含 200 登录页）判为 `DAV_NOT_WEBDAV`。验证：故障注入 `html`。
- [x] 2.5 错误分类与 Retry-After：401/403 快速失败；429/5xx/507 归为可重试并解析等待时间。验证：单元测试 + 故障注入。

## 3. 凭据

- [x] 3.1 safeStorage 加密保存，数据库只存凭据引用键。验证：真实连接后凭据文件为加密二进制。
- [x] 3.2 加密不可用时拒绝连接，不回退明文。验证：代码路径 + UI 提示。
- [x] 3.3 断开只删凭据、保留连接记录与备份历史。验证：界面断开 → 重连后「已备份 2919」不变。

## 4. 上传管线

- [x] 4.1 远端布局：library.json、对象按 device/uploadId/sha256 隔离、记录按月分目录。验证：单元测试 + 远端实测目录。
- [x] 4.2 新建或加入图库：读回 library.json 校验。验证：真实服务建库与加入。
- [x] 4.3 上传对象 → 读回校验 SHA-256 → 发布记录 → 读回校验记录，全部通过才标记已校验。验证：全量 2,919 张。
- [x] 4.4 断点续传：先读回已分配对象判断上次是否成功，避免重复传输。验证：中断/重跑与保留对象复用。
- [x] 4.5 退避与暂停：可重试错误安排退避，认证失败暂停整轮。验证：故障注入 429/500/截断。
- [x] 4.6 幂等：全部完成后重跑计划为 0。验证：真机重跑计划 0。

## 5. 界面

- [x] 5.1 设置页：连接表单、能力测试结果列表、断开。验证：真实窗口 phaseG。
- [x] 5.2 同步中心：真实状态（全部/已备份/待备份/失败）、上传进度、暂停、重试失败项。验证：真实窗口 phaseG。

## 6. 验证与交付

- [x] 6.1 隔离 WebDAV 测试服务（真实 HTTP + 可注入故障）与验证入口 `--verify-upload`。
- [x] 6.2 全量上传：2,919 张，2,918 一次通过 + 1 张因本地临时目录清理失败，重试后全部已校验。
- [x] 6.3 故障注入：限流 429、PUT 500、PUT 只存一半、GET 截断、登录页、认证失败，逐项确认状态与后续恢复。
- [x] 6.4 独立核对：远端 200 个对象文件名=内容哈希、记录字段与路径范围、2,919 条已校验记录无缺对象/缺记录。
- [x] 6.5 `pnpm typecheck` / `pnpm test`（156 条）/ `pnpm build` 全绿。
- [x] 6.6 产出实施报告并提交。

## 7. 遗留（不属于本阶段）

- [ ] 7.1 新电脑恢复：读取远端记录、按游戏选择下载、恢复后校验（阶段 4）。
- [ ] 7.2 远端孤儿对象保留与清理策略（对象不可变，重复上传会留下旧对象）。
- [ ] 7.3 真实第三方 WebDAV 服务验证（本阶段只用了本机隔离服务）。
- [ ] 7.4 跨电脑端到端恢复验证。
- [ ] 7.5 `appinfo.vdf` v29 字符串池格式，用于未安装游戏的真实名称。
