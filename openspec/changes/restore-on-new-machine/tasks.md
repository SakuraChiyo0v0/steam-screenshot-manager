# 阶段 4 实施任务：新电脑恢复

## 1. 修好独立图库的显示链路（阶段 2 遗留缺口）

- [x] 1.1 图片定位改为来源优先、图库副本兜底。验证：恢复后的机器（无来源）能显示相册与大图。
- [x] 1.2 "可用"判定改为来源存在或图库已有副本。验证：phaseH 中不可用卡片为 0。
- [x] 1.3 游戏封面接受只有图库副本的游戏。验证：phaseH 中 6/6 封面加载。
- [x] 1.4 迁移 v5：`assets.original_filename` 并回填。验证：旧库升级后文件名不再退化为占位文案。

## 2. 远端清单

- [x] 2.1 逐层 PROPFIND 记录目录，读回并校验每份记录。验证：2,919 条记录 0 无效。
- [x] 2.2 按 `(账号, 游戏, 指纹)` 去重成逻辑资产，按游戏汇总数量、体积与"本机已有"。验证：单元测试 + 真机 6 款游戏。

## 3. 恢复执行

- [x] 3.1 下载 → 校验指纹与字节数 → staging + 改名发布。验证：单元测试（内容不一致拒绝发布）。
- [x] 3.2 写入说明文件（含 `restoredFrom` 溯源）、`assets`、`games`、`local_copies`；不创建来源文件记录。验证：单元测试 + 真机 `source_files = 0`。
- [x] 3.3 续传与幂等：已存在且校验通过则跳过。验证：单元测试 + 真机重跑 0 恢复 / 2,919 跳过。
- [x] 3.4 取消后保留已完成部分。验证：单元测试。
- [x] 3.5 恢复后登记 `remote_objects`（verified），避免换机再次备份时重传。验证：单元测试 + 真机 2,919 条登记。
- [x] 3.6 恢复的对象缺失、认证失败等错误分类与失败清单。验证：单元测试 + 代码路径。

## 4. 主进程与界面

- [x] 4.1 恢复任务编排：单实例、进度事件、协作式取消。验证：真机运行与 phaseH。
- [x] 4.2 IPC 通道与输入校验：读取清单、开始恢复、取消、状态。验证：typecheck 与契约。
- [x] 4.3 查看器显示真实的远端备份状态（不再写死"未接入远端存储"）。验证：phaseH 详情显示"已备份到远端"。

## 5. 验证与交付

- [x] 5.1 模拟新电脑：空白用户数据目录 + 空白图库，仅靠远端恢复。
- [x] 5.2 真机恢复：2,919 / 2,919 恢复成功、0 失败；重跑 0 恢复 / 2,919 跳过。
- [x] 5.3 与真实数据基准对账：2,919 / 2,919，0 缺失 0 多余；文件名即指纹抽查 100/100 一致。
- [x] 5.4 恢复后的真实窗口：无来源目录下 6/6 封面、相册 0 不可用、大图正常、原文件名与来源时间正确。
- [x] 5.5 `pnpm typecheck` / `pnpm test`（165 条）/ `pnpm build` 全绿。
- [x] 5.6 产出实施报告并提交。

## 6. 遗留（不属于本阶段）

- [ ] 6.1 真实第三方 WebDAV 服务验证（`nas` 主机解析到 10.0.0.80，但常见 WebDAV 端口都不通，需要 VPN 或正确地址；配置里也没有它的凭据）。
- [ ] 6.2 两台真实电脑之间的端到端恢复（本阶段用空白 profile + 空白图库在同一台机器上模拟）。
- [ ] 6.3 远端孤儿对象保留与清理策略。
- [ ] 6.4 双向同步与删除传播（产品需求明确留待单独设计）。
- [ ] 6.5 `appinfo.vdf` v29 字符串池格式，用于未安装游戏的真实名称。
